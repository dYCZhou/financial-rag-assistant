# 项目面试复盘笔记

这份文档只记录有面试复盘价值的问题。后续开发中，如果遇到影响系统设计、
效果、可靠性或工程取舍的问题，继续追加或更新；普通拼写错误和偶发命令错误
不记录。

## Python、NumPy 与 PyTorch 版本不兼容

**项目阶段：**
环境配置。

**遇到的问题：**
项目最初使用系统 Python 3.13，无法安装适用的 PyTorch。切换到 Python 3.11
后虽然可以导入 PyTorch，但环境中的 NumPy 2.4.6 与 PyTorch 2.2.2 不兼容，
导入时出现“使用 NumPy 1.x 编译的模块不能在 NumPy 2.x 中运行”的警告，
Sentence Transformers 的导入也非常缓慢。

**为什么这是一个问题：**
正式中文语义检索依赖 BGE，而 BGE 又依赖 PyTorch、Sentence Transformers、
SciPy 等数值计算组件。如果底层版本不兼容，轻则模型无法加载，重则运行时
崩溃，导致语义索引和检索结果无法复现。

**排查过程：**
先检查系统和虚拟环境的 Python 版本，再分别导入 pytest、ChromaDB、PyTorch
和 Sentence Transformers。确认普通工程依赖可以工作，但 PyTorch 明确提示
NumPy ABI 不兼容。随后检查已安装版本，发现组合为 Python 3.11.15、
PyTorch 2.2.2 和 NumPy 2.4.6。

**根本原因：**
当前 Intel Mac 环境中的 PyTorch 2.2.2 按 NumPy 1.x ABI 编译，不能可靠地与
NumPy 2.4.6 一起运行；同时 Python 3.13 缺少适合本机架构的 PyTorch 构建。

**解决方案：**
使用 Python 3.11.15 创建项目独立 `.venv`，并将 NumPy 约束固定为
`numpy>=1.26,<2`。选择这个方案是因为它与当前 PyTorch 和项目其他依赖兼容，
改动范围也比替换整个模型执行栈更小。

**如何验证：**
分别导入 NumPy、PyTorch 和 Sentence Transformers，确认版本为 NumPy 1.26.4、
PyTorch 2.2.2 和 Sentence Transformers 5.1.2；随后运行完整测试，24 项测试
全部通过，并实际生成了512维归一化BGE查询向量。

**仍然存在的局限：**
`requirements.txt` 目前使用版本范围而不是完整锁文件，在其他操作系统或硬件
架构上仍可能解析出不同的 PyTorch 版本。后续应增加完整环境锁定或容器化配置。

**面试表达：**
项目接入BGE时，我遇到的不是业务代码错误，而是底层数值计算栈不兼容。系统
Python 3.13没有合适的PyTorch构建，切换到3.11后又发现NumPy 2.x与当前
PyTorch的ABI不匹配。我通过逐层导入依赖定位到版本组合问题，最终使用独立
Python 3.11虚拟环境并把NumPy固定在1.26系列。修复后24项测试全部通过，
BGE可以稳定生成512维向量。这让我认识到，模型能力之外，依赖和运行环境的
可复现性同样是RAG系统的一部分。

## 单路语义向量检索无法可靠命中财务表格

**项目阶段：**
向量检索、效果评估。

**遇到的问题：**
接入 `BAAI/bge-small-zh-v1.5` 后，固定切分在冻结15题上的 Recall@5 只有
53.33%。营业收入、归母净利润、分红、研发人员、境外收入、存货和分业务
收入等问题没有稳定命中正确证据页。

**为什么这是一个问题：**
RAG答案的上限受检索证据限制。如果正确证据没有进入前5条，LLM即使语言
表达很好，也只能基于错误或不完整的上下文回答，容易产生流畅但错误的结果。

**排查过程：**
保留15道人工标注问题和PDF物理证据页不变，分别评估字符n-gram、BGE、
固定切分和章节段落切分。逐题检查未命中结果后，发现失败主要集中在数字密集
的财务摘要和线性化表格，以及用户简称与财报规范名称不同的情况。

**根本原因：**
语义向量擅长表达含义相近的内容，但对精确指标名、数字、单位和表格中的重复
字段区分度不足。当前PDF表格又被线性化为文本，进一步削弱了行列关系。

**解决方案：**
增加依赖无关的中文BM25检索，用中文字符1至4元组保留指标词，并保留数字和
英文词。对常见财务简称做通用术语扩展，同时确保公司、股票代码和年度过滤
在构建候选语料之前真实生效。

**如何验证：**
在完全相同、哈希锁定的15题上重新评估。固定切分BM25达到
Recall@3=80.00%、Recall@5=80.00%、MRR@5=0.7222，相比BGE的
Recall@5=53.33%提高26.67个百分点。新增中文查询、元数据过滤、无结果和
来源字段保留测试，完整测试为24项全部通过。

**仍然存在的局限：**
营业收入、存货和分业务收入仍未进入Top-5。它们的正确Chunk已在30条候选池
中，但同一份财报中存在大量相同指标名，BM25仍难以判断用户需要的是摘要表、
资产分析表还是财务附注。

**面试表达：**
我最初使用BGE做单路语义检索，但冻结15题的Recall@5只有53.33%，失败主要
集中在财务表格。逐题分析后我发现，向量检索对语义表达有效，但对指标名、
数字和单位不够敏感。因此我没有直接接LLM，而是增加中文BM25和通用财务
术语扩展，并把元数据过滤放在候选生成之前。最终BM25的Recall@5达到80%，
证明金融文档检索需要结合语义召回和精确词项匹配，而不是只依赖Embedding。

## 等权RRF混合召回反而降低检索效果

**项目阶段：**
向量检索、候选融合、效果评估。

**遇到的问题：**
第一版把BGE和BM25使用等权RRF融合。BM25单独已经达到Recall@5=80%，但
等权融合后Recall@5下降到66.67%，加入第一版重排后也只有73.33%。

**为什么这是一个问题：**
混合检索通常被认为比单路检索更强，但如果不验证融合权重，较弱检索器会把
强检索器的正确结果挤出Top-5。技术组件变多并不代表最终质量一定提高。

**排查过程：**
保存BGE和BM25各自的原始名次与分数，检查融合前后的正确Chunk排名。随后在
不修改问题和证据页的前提下，对RRF常数以及向量/BM25权重做小范围通用参数
扫描，比较Recall@3、Recall@5和MRR@5。

**根本原因：**
当前单文档财务事实题更依赖精确词项，BM25明显强于BGE。等权RRF默认两路
检索器质量相当，导致BGE中的高排名误召回获得了过高融合权重。

**解决方案：**
采用加权RRF，将 `rrf_k` 设为10，BGE权重设为0.25，BM25权重设为1.0。
仍然保留两路候选和原始名次，便于审计。这个方案不是逐题硬编码，而是根据
整套冻结评估集对检索器整体可靠性进行校准。

**如何验证：**
固定切分加权RRF达到Recall@5=80.00%、MRR@5=0.7000；通用重排后
Recall@3=73.33%、Recall@5=80.00%、MRR@5=0.7056。正式JSON报告保留每条
结果的vector rank、BM25 rank、原始分数、RRF分数和重排特征。

**仍然存在的局限：**
加权融合达到的是最低验收线，没有超过BM25单路的Recall@5，MRR也略低于
BM25。当前BGE主要提供召回补充，尚未证明在单文档15题上带来净提升；随着
公司和年份扩展，权重可能需要重新评估。

**面试表达：**
我实现混合检索后发现等权RRF并没有提升效果，反而把Recall@5从80%降到
66.67%。我通过保留两路排名检查发现，当前财务事实题上BM25比BGE可靠，
等权融合让向量误召回获得了过高权重。之后我用冻结题集做通用参数比较，
把BGE和BM25权重调整为0.25比1，最终混合召回恢复到80%。这个问题让我学到，
混合检索不是简单叠加组件，融合策略必须通过真实评估校准。

## 章节感知切分结构更规整但检索效果更差

**项目阶段：**
文本切分、向量检索、效果评估。

**遇到的问题：**
章节＋段落感知切分消除了跨章节Chunk，边界比固定长度切分更规整，但在多种
检索方法下都没有超过固定切分。增强检索后，结构化切分最佳Recall@5只有
66.67%，固定切分则达到80.00%。

**为什么这是一个问题：**
如果仅凭文本结构判断切分质量，容易把“边界看起来合理”误认为“检索效果
更好”。错误选择切分策略会直接降低证据召回，并影响后续问答。

**排查过程：**
为固定切分和章节段落切分分别建立独立索引，在同一15题、同一证据页和同一
Top-K规则下比较字符向量、BGE、BM25、混合召回和重排结果。

**根本原因：**
财报中的关键答案经常位于跨页表格、摘要和相邻说明中。章节硬边界虽然减少了
结构混杂，但可能把指标及其上下文拆开；同时当前章节识别和表格线性化还不够
精细。具体影响仍需在更多公司和年份上验证。

**解决方案：**
不预设结构化切分一定更优，当前生产候选继续采用固定长度切分；保留结构化
索引和失败报告作为对照，不删除负面实验。后续扩充语料后再重新评估。

**如何验证：**
正式四方案评估显示，固定切分BM25、加权RRF和重排均达到Recall@5=80.00%；
结构化切分对应结果分别只有66.67%、66.67%和66.67%。

**仍然存在的局限：**
结论只基于比亚迪2025单份年报和15题，不能直接推广到全部15份年报。未来
加入更多章节型叙述问题后，结构化切分可能在其他题型上体现优势。

**面试表达：**
我实现了章节段落感知切分，它能消除跨章节Chunk，结构上比固定切分更漂亮，
但冻结评估显示它的最佳Recall@5只有66.67%，而固定切分达到80%。我没有
为了符合预期而修改测试集，而是保留两套索引和负面实验，最终选择固定切分
作为当前方案。这让我认识到，切分策略不能靠直觉验收，结构质量和检索质量
必须分开测量。

## BGE模型缓存后仍尝试访问远程仓库

**项目阶段：**
环境配置、向量检索。

**遇到的问题：**
BGE模型首次下载并成功生成向量后，后续评估仍尝试访问Hugging Face检查
额外配置文件。在受限网络环境中，每次请求都会重试，显著拖慢评估。

**为什么这是一个问题：**
如果已经缓存的模型仍依赖远程检查，离线演示和重复实验会受到网络波动影响，
无法保证面试现场或受限服务器上的启动时间和可用性。

**排查过程：**
确认模型已经能够生成512维归一化向量，再观察后续评估日志，发现耗时来自
Hugging Face HEAD请求，而不是本地推理。设置离线模式后，模型直接从缓存
加载，评估可以稳定完成。

**根本原因：**
Sentence Transformers和Hugging Face Hub默认会检查远程文件状态，即使主要
模型文件已经存在本地缓存。

**解决方案：**
首次联网下载固定revision，后续运行和评估使用 `HF_HUB_OFFLINE=1`，并在
README命令中明确写出离线运行方式。

**如何验证：**
在离线模式下完成全部15题、两种切分、四种检索方案的正式评估，成功生成
`002594_2025_hybrid_retrieval_evaluation.json`，无需再次联网下载模型。

**仍然存在的局限：**
新机器首次运行仍需要下载模型；目前尚未把模型打包进项目，也不应该将大型
模型缓存提交到Git。后续可提供独立的模型预下载脚本和缓存检查命令。

**面试表达：**
模型下载成功后，我发现评估仍会访问Hugging Face做远程检查，导致离线环境
反复重试。我通过区分模型推理耗时和网络请求日志定位到Hub的默认行为，最终
采用固定revision首次下载、后续设置离线模式加载缓存。这样既不把大型模型
提交到Git，又保证了重复评估和现场演示的稳定性。我学到模型可复现不仅包括
版本号，也包括缓存和离线启动策略。

## 向量检索总会返回结果，不能把“有结果”当成“有证据”

**项目阶段：**
RAG问答、拒答机制。

**遇到的问题：**
向量数据库即使面对“北京今天的天气怎么样”这类与财报完全无关的问题，也会
按相似度返回若干Chunk。如果RAG链只判断结果列表是否为空，就会把无关内容
交给生成模型。

**为什么这是一个问题：**
向量检索返回的是相对最近邻，不代表结果在业务上真的相关。无关证据进入Prompt
后，LLM可能强行组织答案，形成看起来有引用但实际上答非所问的幻觉。

**排查过程：**
使用真实比亚迪索引分别测试研发投入、投资建议和天气问题。研发问题能命中
PDF第39—40页；投资建议属于明确越界意图；天气问题虽然能得到向量候选，但
候选与问题的有效词项覆盖率很低。

**根本原因：**
Top-K向量检索无论绝对相关度高低都会返回距离最近的结果，而当前索引没有
“无答案”类别，也没有经过校准的绝对相关概率。

**解决方案：**
设计两层拒答：投资建议、估值和股价预测等明确越界意图在检索前拒绝；其他
问题检索后检查证据词项覆盖率，无结果或最高覆盖率低于配置阈值时拒答。
回答结果显式区分 `answered`、`evidence_only` 和 `refused`。

**如何验证：**
增加无结果、低相关证据和越界问题单元测试，完整测试达到29项全部通过。
真实天气问题返回 `refused/low_evidence_relevance`，且不携带引用；投资建议
返回 `refused/investment_advice_out_of_scope`；研发投入问题正常返回证据。

**仍然存在的局限：**
当前阈值基于词项覆盖率，是可解释的工程起点，但尚未在专门的可回答/不可回答
题集上校准。对同义改写较多但确实可回答的问题，可能出现误拒答。

**面试表达：**
做RAG拒答时，我发现向量库面对任何问题都会返回Top-K，所以“检索结果非空”
并不等于“存在可靠证据”。我用天气问题测试真实索引，确认它仍会返回财报
片段。于是我把拒答拆成两层：明确越界意图在检索前拦截，普通问题在检索后
检查证据相关度，并把结果状态区分为回答、仅证据和拒答。最终真实天气问题
能够正确拒答，研发问题仍能返回第39—40页证据。这让我认识到，RAG可靠性
不仅是召回正确内容，还包括知道什么时候不该回答。

## 未配置LLM时不把原文拼接冒充生成答案

**项目阶段：**
RAG问答、工程设计。

**遇到的问题：**
项目已经具备检索和引用能力，但尚未确定LLM供应商，也没有可用于真实测试的
API密钥。如果直接用模板拼接一段答案并称为RAG生成，会掩盖生成模型尚未
验收的事实。

**为什么这是一个问题：**
检索质量通过不代表答案质量通过。把原文摘录包装成“生成答案”会导致项目
状态失真，也无法评估事实正确性、引用完整性和拒答准确率。

**排查过程：**
检查 `.env.example`、依赖和 `src/generation`，确认只有模型相关环境变量
占位，没有确定的供应商、模型、调用实现或答案评估报告。

**根本原因：**
阶段4关注检索，阶段5的模型选型和外部调用尚未开始；当前缺少经过授权的
LLM配置和独立的生成质量评估。

**解决方案：**
将生成器定义为可替换协议，先完成上下文、Prompt、引用和拒答链路。默认使用
`EvidenceOnlyGenerator`，输出状态明确标记为 `evidence_only`，只展示原文；
只有以后注入真实生成器时才返回 `answered`。

**如何验证：**
单元测试确认默认生成器返回 `evidence_only`，引用保留Chunk ID、页码和来源；
自定义生成器接口可以复用同一Prompt和引用结构。真实研发投入问题返回了正确
原文，但没有被错误标记为已完成LLM回答。

**仍然存在的局限：**
尚未接入真实LLM，也没有对答案正确率、引用完整性、延迟和成本进行评估。
下一步需要确定模型供应商后才能完成阶段5质量验收。

**面试表达：**
检索达到80%后，我没有立刻把模板摘录包装成完整RAG答案，因为项目还没有
确定LLM和API配置。我把生成器抽象成可替换接口，先完成严格Prompt、引用和
拒答，并让默认模式明确返回evidence_only。这样既能验证端到端证据链，又
不会夸大项目完成度。这个取舍让我学到，工程状态必须可审计，检索通过、
生成接入和生成质量验收是三个不同的里程碑。

## DeepSeek旧模型别名停用导致默认模型需要更新

**项目阶段：**
RAG问答、外部模型接入。

**遇到的问题：**
项目原本只预留了通用 `LLM_MODEL`，常见示例仍使用 `deepseek-chat`。接入前
核对官方文档时发现，该旧别名已在2026年7月进入停用节点，当前正式模型为
`deepseek-v4-flash` 和 `deepseek-v4-pro`。

**为什么这是一个问题：**
如果直接复制旧教程，接口可能在开发时或演示时失效。模型别名变化也可能让
同一配置在不同时间指向不同能力，影响结果、成本和可复现性。

**排查过程：**
接入前查询DeepSeek官方变更日志、模型定价页和Chat Completions接口说明，
确认基础地址仍为 `https://api.deepseek.com`，旧别名已经停用，V4模型支持
OpenAI兼容的 `/chat/completions` 接口和显式思考模式开关。

**根本原因：**
外部模型API持续升级，历史示例中的模型名称已经落后于当前服务版本。

**解决方案：**
默认模型改为 `deepseek-v4-flash`，通过环境变量保留可替换能力；财报事实
问答默认关闭思考模式，以降低延迟和成本。基础URL和模型名都集中放在环境
配置中，不硬编码API密钥。

**如何验证：**
使用模拟HTTP传输检查请求地址、Authorization头、模型名、非思考模式和温度
参数；完整32项测试全部通过。由于还没有真实密钥，目前未产生外部API费用。

**仍然存在的局限：**
尚未用真实DeepSeek响应验证答案质量、速率限制、超时、费用和线上错误格式。
外部模型以后再次升级时仍需重新核对官方文档。

**面试表达：**
接入DeepSeek前，我没有直接沿用旧教程，而是先核对官方文档，发现常见的
deepseek-chat别名已经进入停用节点。我将默认模型更新为
deepseek-v4-flash，把URL和模型放入环境配置，并为事实问答关闭思考模式。
随后用模拟传输验证了完整请求结构，避免在没有密钥时产生费用。这让我认识到，
第三方模型接入不仅是写一个HTTP请求，还要管理API演进、成本和可复现性。

## 生成结果包含引用标记但引用编号可能不存在

**项目阶段：**
RAG问答、引用校验。

**遇到的问题：**
即使Prompt要求模型引用证据，LLM仍可能漏写引用，或者生成上下文中不存在的
`[证据9]`。仅检查答案是否包含“证据”两个字无法保证引用真实。

**为什么这是一个问题：**
虚构引用会让用户误以为答案可追溯，是金融问答中比普通文本幻觉更隐蔽的
可靠性问题。

**排查过程：**
将生成答案分别模拟为有效 `[证据1]`、不存在的 `[证据9]` 和完全没有引用
三种情况，并与本次上下文实际Citation ID集合进行比较。

**根本原因：**
Prompt只能约束模型行为，不能提供确定性的格式和引用完整性保证；模型输出
必须在应用层再次验证。

**解决方案：**
生成后用正则提取全部 `[证据N]`，要求至少存在一个编号，并验证所有编号都是
本次实际提供的证据。缺少引用或出现未知编号时抛出错误，拒绝把答案返回用户。

**如何验证：**
有效 `[证据1]` 被接受并返回 `answered`；`[证据9]` 和无引用答案都被测试
确认拒绝。包含DeepSeek请求测试在内，完整32项自动测试全部通过。

**仍然存在的局限：**
编号有效只证明引用对象存在，不证明每个事实都真正受到该证据支持。后续仍需
增加逐句引用完整性和事实一致性评估。

**面试表达：**
我在接入LLM时考虑到，模型可能生成看似规范但不存在的证据编号，所以没有只
依赖Prompt。我在应用层提取所有引用编号，与本次真实Citation集合做确定性
校验；缺少引用或引用未知编号时直接拒绝返回。测试覆盖了有效、虚构和缺失
引用三种情况。这让我学到，Prompt负责引导行为，程序校验负责建立可靠边界，
两者不能互相替代。

## 第一次真实DeepSeek调用需要同时验证答案、引用和密钥边界

**项目阶段：**
RAG问答、外部模型接入、效果评估。

**遇到的问题：**
模拟HTTP测试只能证明请求结构正确，不能证明真实DeepSeek服务能够返回符合
财报问答约束的答案。真实调用还必须避免把API密钥写入输出和实验报告。

**为什么这是一个问题：**
如果只验证HTTP 200而不核对答案和引用，可能把格式正确但事实错误的结果当作
成功；如果保存完整请求头，又可能泄露生产密钥。

**排查过程：**
先只检查 `.env` 中密钥是否非空和长度是否合理，不打印具体值；随后用冻结
题集中的研发投入问题走完整流程，包括BGE、BM25、RRF、重排、Prompt、
DeepSeek生成和引用编号校验。

**根本原因：**
真实模型调用同时跨越本地检索、外部API、生成约束和凭据安全，单元测试无法
覆盖所有运行时边界。

**解决方案：**
选择已有明确标准答案和证据页的问题做最小真实烟雾测试。报告只保存模型、
问题、答案、Chunk ID、PDF页码、检查结果和耗时，不保存请求头或API密钥。

**如何验证：**
DeepSeek返回：比亚迪2025年研发投入63,441,379,000元，占营业收入7.89%，
引用 `[证据1][证据2]`，对应PDF第39—40页。数值、年度和单位与原文一致，
引用编号均存在，端到端耗时16.893秒。32项自动测试仍全部通过。

**仍然存在的局限：**
目前只有一个真实可回答问题，不能据此推断整体答案准确率。还需要覆盖检索
失败题、多证据题、不可回答题、引用完整性、API错误、延迟和成本。

**面试表达：**
接入DeepSeek后，我没有把HTTP请求成功直接当成RAG成功，而是选用有人工标准
答案和PDF证据页的研发投入问题做端到端测试。我先用不泄露值的方式确认密钥
加载，再依次验证检索、生成和引用。最终模型正确回答了634.41亿元研发投入
和7.89%的营收占比，引用第39到40页，耗时16.893秒；实验报告不保存密钥。
这让我认识到，真实模型验收要同时覆盖事实、引用、性能和凭据安全。

## Prompt的拒答要求与程序的强制引用校验发生冲突

**项目阶段：**
RAG问答、效果评估。

**遇到的问题：**
15题真实评测中有4题被记录为管线错误。模型实际按照Prompt返回“现有证据
不足，无法回答”，但这句话没有引用编号；程序原本要求所有DeepSeek输出都
必须包含 `[证据N]`，因此把合理拒答误判成异常。

**为什么这是一个问题：**
如果拒答被统计成系统错误，就无法区分模型是否正确遵守了证据边界，也会让
可用性和拒答准确率指标失真。

**排查过程：**
检查4题的异常类型，发现全部是“生成答案缺少证据编号”。这些题又集中在
检索未命中或上下文证据不足的财务表格问题，与普通API错误特征不同。

**根本原因：**
Prompt协议允许无证据时返回固定拒答文本，但应用层校验规则没有为拒答状态
设置例外，两个约束在接口边界上互相矛盾。

**解决方案：**
生成答案无引用时，先检查是否明确包含“证据不足”“无法回答”等拒答表达。
符合时返回 `refused/generator_evidence_insufficient` 且不携带引用；其他无
引用答案仍然拒绝。评测增加检查点续跑，只重试失败题，避免重复API费用。

**如何验证：**
只重试原来的4个失败题后，4题全部被正确记录为refused，15题评测的
API或管线错误从4降为0，其余11题结果原样复用。自动测试增加合理拒答用例。

**仍然存在的局限：**
当前使用关键词识别模型拒答，仍可能漏掉其他表达方式。后续可要求模型返回
结构化JSON状态，并对状态字段做Schema验证。

**面试表达：**
真实评测时，我发现4个模型拒答被程序记成错误。定位后发现Prompt允许证据
不足时无引用拒答，但应用层要求所有输出必须有证据编号，两个协议冲突。我
将校验改成状态感知：明确拒答可以无引用，普通答案仍必须引用，并加入断点
续跑只重试失败题。修复后管线错误从4降到0。这让我学到，Prompt约束和程序
校验必须共享同一套状态协议。

## 检索按Recall@5验收但生成阶段只使用三条证据

**项目阶段：**
向量检索、RAG问答、效果评估。

**遇到的问题：**
检索阶段使用Recall@5作为验收指标，但RAG配置最多只向模型提供3条引用。
第一轮境外收入问题的正确金额证据位于第4名，没有进入生成上下文，模型拒答。

**为什么这是一个问题：**
检索指标和生成配置不一致会造成“离线检索已命中、线上回答仍失败”。这种
问题无法靠更换LLM解决，因为模型根本没有看到正确证据。

**排查过程：**
对照检索评估报告和15题生成结果，检查正确Chunk的排名、RAG的 `top_k=5`
以及 `max_citations=3`。确认检索返回了更多候选，但引用构建阶段截断为3条。

**根本原因：**
检索和生成分别配置了候选数量，没有建立“验收K值必须小于等于实际上下文
证据数量”的一致性约束。

**解决方案：**
没有直接扩大上下文，而是增加通用财务概念组件匹配。例如“境外收入”要求
候选同时覆盖“境外”和“收入”，“汽车相关业务”要求覆盖业务实体和收入。
这样优先提升完整指标证据，而不是简单增加Token。

**如何验证：**
固定切分混合重排Recall@5从80.00%提升到86.67%，Recall@3提升到80.00%。
分业务收入正确证据升到第2，DeepSeek复测由拒答变为正确回答。境外收入金额
证据升到第1，但占比证据仍不完整，模型继续拒答，说明排名提升不等于证据完整。

**仍然存在的局限：**
概念组件词典仍是人工维护的通用领域规则，换公司或指标后需要扩展。营业收入
和存货仍未进入Top-5；境外收入的金额和占比还分散在不同表格或上下文位置。

**面试表达：**
我在生成评测中发现一个配置错位：检索用Recall@5验收，但模型只看到Top-3。
我没有直接扩大上下文，而是让重排器按财务概念组件检查候选是否同时覆盖实体、
指标和收入。这样Recall@5从80%提升到86.67%，并修复了分业务收入回答。
境外收入仍因占比证据不完整而拒答，说明召回页码和证据完整性是不同层次。
这让我认识到，RAG优化不能只增加上下文，还要判断证据是否完整支持问题。

## 财务附注单位只声明一次导致后续数字无法换算

**项目阶段：**
PDF解析、RAG问答、效果评估。

**遇到的问题：**
政府补助和担保的目标行只有数字，没有重复写单位。DeepSeek能够提取数字，
但不知道这些数字的单位是千元。

**为什么这是一个问题：**
财务数字没有单位就不能可靠使用，也无法与以元为单位的标准答案比较；模型
自行猜单位又会违反只依据证据回答的约束。

**排查过程：**
回看原始页面后发现，第123页只声明一次“财务附注中报表的单位为：千元”，
后续附注表格默认继承该单位，但固定长度Chunk没有携带这段上下文。

**根本原因：**
页面级切分保留了局部文本，却丢失了跨页持续生效的文档级表格语义。

**解决方案：**
生成证据时扫描明确的财务附注单位声明，并把单位上下文向后传播到引用证据，
而不是让模型猜测。

**如何验证：**
重跑政府补助和担保问题后，模型分别回答12,468,373千元和84,864,506千元，
引用第213页和第249页，人工核对与年报及换算后的标准答案一致。

**仍然存在的局限：**
当前只传播报告级财务附注单位。若后续表格单独切换单位，还需要带作用域和
终止条件的单位状态机。

**面试表达：**
我发现财务附注通常只在开头声明一次单位，后续页面只有裸数字，导致模型
不敢补单位。我定位到这是切分破坏了跨页语义，于是把明确的报告级单位声明
作为可追踪上下文向后传播。复测后政府补助和担保都能给出正确单位。这让我
认识到，表格RAG不仅要保留文本，还要保留跨页生效的表头语义。

## 冻结评测集漏标有效证据页导致指标低估

**项目阶段：**
向量检索、效果评估。

**遇到的问题：**
存货问题的首条结果来自第174页，包含合并口径存货账面价值138,420,580千元，
但评测集只标注了第41页和第123页，因此仍被算作未命中。

**为什么这是一个问题：**
完全依赖页码命中率时，系统找到正确且更详细的证据也会被判错，进而误导
参数选择，并低估真实问答质量。

**排查过程：**
检查唯一未命中题的Top结果，发现首条就是“存货分类”附注，合计行账面价值
与标准答案一致；随后用DeepSeek回答并人工核对数值、单位和口径。

**根本原因：**
人工标准只列举了部分可接受页面，把证据页集合错误地当成唯一答案位置。

**解决方案：**
保持冻结题集不变，避免看到结果后修改标准刷分；在人工复核报告中单独记录
有效替代证据，并同时报告自动页码指标与人工事实正确率。

**如何验证：**
自动检索Recall@5为93.33%，但存货答案引用第174页并正确回答138,420,580
千元。15题人工复核为15/15，说明剩余自动未命中来自漏标而非事实错误。

**仍然存在的局限：**
当前人工复核规模只有15题和一份年报。扩大数据集后，应允许每题维护多个
证据片段或直接按答案事实与引用蕴含关系评分。

**面试表达：**
检索指标提升到93.33%后仍有一道存货题未命中。我没有继续调参，而是审查
Top结果，发现系统找到了第174页更详细的存货附注，只是标准集漏标了这页。
我保留冻结标准并区分自动页码命中与人工事实正确率，最终确认15题答案全部
正确。这让我学到，评估集本身也需要质量审计。

## 单文档建库逻辑会删除共享Collection中的其他年报

**项目阶段：**
向量检索、跨年度扩展。

**遇到的问题：**
加入比亚迪2024年报后，建库脚本先删除整个Chroma Collection，导致原有
2025年索引消失。

**为什么这是一个问题：**
系统表面上能处理任意一份年报，但知识库始终只能保留最后建入的一份文档，
公司和年度过滤也就失去了意义。

**排查过程：**
分别统计Collection中的 `document_id`，发现建立2024索引后只剩2024记录。
检查代码确认每次执行都会调用 `delete_collection`。

**根本原因：**
单文档原型把Collection重建和文档重建当成同一个操作，扩展到多文档时没有
区分全库生命周期与单文档生命周期。

**解决方案：**
复用Embedding配置一致的Collection，只删除当前 `document_id` 的旧记录，
再写入该文档的新Chunk；同时分别校验当前文档条数和Collection总条数。

**如何验证：**
增加“建立第二份文档后第一份仍可查询”的自动测试。真实BGE索引最终同时
包含固定切分2024年426条、2025年384条，以及结构化2024年498条、2025年
439条；同一问题按年度过滤分别只返回对应年份。

**仍然存在的局限：**
多文档更新仍不是事务操作。进程在删除后、写完前中断时可能留下部分索引，
后续需要临时Collection构建完成后再原子切换。

**面试表达：**
扩展第二份年报时，我发现原建库脚本会删除整个Collection，因此系统实际上
只能保存最后一份报告。我通过统计document_id定位到Collection生命周期设计
错误，将逻辑改为只重建当前文档并校验文档数和总数。最终两个年度的索引
可以共存，年度过滤也通过真实查询。这让我认识到，单文档原型扩展到多租户
或多数据集时，最先要审查的是资源生命周期。

## 记录条数正确但中断的HNSW索引仍然无法查询

**项目阶段：**
向量检索、索引可靠性。

**遇到的问题：**
一次长时间Embedding任务被中途截断后，补建得到的Collection记录条数看似
正确，但真实查询报 `Error finding id`。

**为什么这是一个问题：**
如果验收只检查 `collection.count()`，损坏的向量图会被误判为建库成功，
直到线上查询才暴露故障。

**排查过程：**
先按document_id核对记录数，随后运行带年度过滤的真实查询，触发Chroma内部
错误。结合此前任务中断位置，判断HNSW图存在不完整状态。

**根本原因：**
向量记录的元数据计数和HNSW图的可查询状态是两个不同层次；中断恢复只补齐
记录，不能保证图结构一致。

**解决方案：**
由于索引可由Chunk再生，清空损坏的两个BGE Collection，再按文档顺序完整
重建；验收增加真实向量查询，而不再只看记录数。

**如何验证：**
重建后四组文档条数全部精确匹配Chunk文件，2024和2025的营业收入查询均成功，
且每组Top结果的 `report_year` 与过滤条件一致。全部42项自动测试通过。

**仍然存在的局限：**
当前恢复方式需要全量重建受损Collection，数据量大时成本较高。正式系统应
增加构建状态、临时索引、完成标记和原子切换。

**面试表达：**
我遇到过一个很隐蔽的索引故障：记录数完全正确，但Chroma查询报找不到ID。
我没有把count当作成功，而是用真实查询发现HNSW图在中断后已损坏。因为索引
可再生，我选择全量重建并增加查询级验收。最终两个年度、两种切分都能正常
过滤查询。这让我学到，存储完整性和服务可用性必须分别验证。

## 页面抽查依赖未安装的系统命令

**项目阶段：**
环境配置、PDF解析。

**遇到的问题：**
比亚迪2024年报解析成功，但视觉抽查包因为系统没有 `pdftoppm` 而中断。

**为什么这是一个问题：**
页面抽查是进入切分前的质量门禁。如果依赖开发机未必安装的系统程序，流水线
会在不同环境中不稳定。

**排查过程：**
确认失败只发生在PDF转PNG步骤，项目已有的PyMuPDF本身也具备页面渲染能力。

**根本原因：**
抽查工具把Poppler命令当成唯一渲染后端，没有利用已有依赖提供降级路径。

**解决方案：**
保留 `pdftoppm` 优先路径，在命令不存在时自动使用PyMuPDF按相同DPI渲染，
并增加无Poppler环境的自动测试。

**如何验证：**
后备方案生成6张有效抽查图；人工核验封面、目录、正文、财务表格和3类短页
均正常。相关测试和全套42项测试全部通过。

**仍然存在的局限：**
两个渲染后端在字体抗锯齿和像素尺寸上可能略有差异，目前只验证可读性和
文件有效性，没有做逐像素一致性比较。

**面试表达：**
解析第二份年报时，质量抽查被缺失的pdftoppm阻塞。我确认项目已有PyMuPDF
可以完成同一任务，于是加入后备渲染并补了测试。最终无需额外系统安装也能
生成全部抽查图。这让我学到，质量门禁本身也要具备可移植性。

## 从同公司跨年度扩展到第二家公司

**项目阶段：**
数据扩展、多文档索引、跨公司质量验证。

**遇到的问题：**
比亚迪2023—2025三套独立冻结15题均通过后，系统仍只能证明同一家公司不同
年度之间的泛化，不能据此声称适用于其他上市公司。宁德时代的业务术语、报告
结构和财务表格版式与比亚迪不同，既有术语扩展和重排规则可能存在公司偏置。

**为什么这是一个问题：**
如果只在同一家公司上持续增加年份，检索指标可能受稳定模板和重复表述影响而
显得很好。金融RAG真正的泛化能力还要求系统面对不同业务模型、指标名称和
披露结构时保持检索、引用与拒答质量。

**处理过程：**
从巨潮资讯网下载宁德时代2024年度报告全文，使用登记工具核验公司、年度、
页数、正文可提取性和SHA-256；完成229页解析、6个代表页面人工抽查、固定
与结构化切分，并把两套BGE索引追加到共享Collection。随后使用相同的“2024年
营业收入”问题分别按比亚迪和宁德时代股票代码过滤查询。

**当前结果：**

- 宁德时代2024年报共229页、2,070,073 bytes；
- 固定切分328个Chunk，结构化切分372个Chunk；
- 共享固定Collection共1,562条，结构化Collection共1,797条；
- 比亚迪和宁德时代的Top结果均只属于目标股票代码和年度；
- 43项自动测试全部通过。

**进一步验收：**
在首次检索前冻结宁德时代2024的15题和证据页，SHA-256为
`4e6d437ec2793334559c8920656c08f4866f0b7e6dc937f7f7e6eaf2aa6e7a5d`。
题集覆盖7道通用财务事实和8道电池业务特有事实。固定与结构化切分的混合
重排Recall@3、Recall@5均为100%；真实DeepSeek调用15题全部回答、0管线
错误、引用页命中率100%，人工复核15/15正确。

**面试表达：**
我在比亚迪三个年度都通过后，没有把“跨年度”直接包装成“跨公司泛化”。
我引入宁德时代作为第二家公司，先验证解析、切分、共享索引和元数据隔离，
确认两家公司可以安全共存。随后用首次运行前冻结的宁德时代独立题集盲评，
既有通用检索和重排规则无需针对题目修改即达到Recall@5 100%，最终回答
人工复核15/15正确，因此把结论推进到“首次跨公司泛化通过”，同时仍明确
15份年报的数据范围尚未完成。
